import 'package:flutter/material.dart';

class BoardSettingItemContainer extends StatelessWidget {
  final dynamic callback;
  BoardSettingItemContainer({required this.callback});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}